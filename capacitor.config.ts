import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'about_me_lab_ex_6',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
